#include <iostream>
#include "pilas.h"
#include "colas.h"
using namespace std;

/*NOTA IMPORTANTE LEER:
 * Las funciones de la pila(push, pop, imp, mostrar)van todas minusculas
 * Las funcines de la cola(push, pop, imp, mostrar)van con inicial mayuscula
 */

int main() {
	push('B');
	push('y');
	push('r');
	pop();
	imp();
	mostrar();
	imp();
		Push('a');
		Push('b');
		Push('c');
		Imp();
		Pop();
		Top();
		Mostrar();

	return 0;
}
